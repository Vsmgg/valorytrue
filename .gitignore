# Claude Code local settings — contains local permission rules and, historically,
# secrets pasted into command allowlists (DB connection strings, admin passwords).
# Never commit this, especially to a public repo.
.claude/

# Dependencies / build output
web/node_modules
web/dist
web/dist-ssr
node_modules
dist
dist-ssr

# Env / secrets
web/.env*
web/.dev.vars
web/.vercel
web/.wrangler
.env*
.dev.vars
.vercel
.wrangler

# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Editor / OS
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
